package nieblas.julio.dclaro

import android.annotation.SuppressLint
import android.content.Intent
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Button
import android.widget.ImageButton
import nieblas.julio.dclaro.R.id.btn_agregafactura

class SeleccionFacturasActivity : AppCompatActivity() {
    @SuppressLint("MissingInflatedId")
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_seleccion_facturas)
        /// ocupa ser botton
       // val btn_agregafactura: ImageButton = findViewById(btn_agregafactura)
       // R.id.btn_agregafactura.setOnClickListener{
            //val intent: Intent = Intent(this,SeleccionFacturasActivity  ::class.java)
         //   startActivity(intent)

       // }
    }
}

